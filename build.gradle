// AGP 9 has built-in Kotlin enabled by default and depends on KGP 2.2.10 at
// runtime. The org.jetbrains.kotlin.android plugin is NOT compatible with the
// new DSL, so it is deliberately absent here — do not add it back.
plugins {
    id 'com.android.application' version '9.3.2' apply false
}
